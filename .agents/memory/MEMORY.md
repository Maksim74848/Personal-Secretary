- [AI provider setup](ai-provider.md) — OPENAI_API_KEY holds a Groq key (gsk_); must use Groq base URL with OpenAI SDK
- [Telegram polling](telegram-polling.md) — polling state lives in telegramState.ts, imported by telegram.ts and system.ts to avoid circular deps
- [Auth setup](auth-setup.md) — Replit Auth (OIDC/PKCE) fully wired; AuthUser type lives in lib/api-zod/src/auth.ts (not generated by orval)
- [replit-auth-web lib](replit-auth-web.md) — composite lib; imports AuthUser from @workspace/api-zod (not api-client-react); needs vite devDep for import.meta.env types
- [User scoping pattern](user-scoping.md) — all data routes: guard with `if (!req.user) 401`; filter DB with eq(table.userId, req.user.id); insert with userId: req.user.id
